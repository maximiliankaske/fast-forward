import { Template } from "@/types/templates";
import React from "react";

// TODO:

interface Props {
  organizationId: string;
  template: Template;
}

const SurveyThumbnail = ({ organizationId, template }: Props) => {
  return <div></div>;
};

export default SurveyThumbnail;
