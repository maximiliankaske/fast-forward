import { default as Layout } from "../components/layout/MDXLayout";

export default (props) => <Layout {...props} />;

# Getting Started

### Welcome to Fast Feedback! 👋🏼

To get started adding feedback to your site, retrieve the site ID
from the URL and place this iframe inside your site.

```tsx
const test = () => {
  const [state, setState] = useState<string>();
};
```

> "About callouts they are called in Notion.io"

export const meta = {
  title: "Docs",
  author: "Maximilian Kaske",
};
